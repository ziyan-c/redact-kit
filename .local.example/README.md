# Redact Kit Local Private Config

Copy files from this directory into `.local/` when you need machine-specific
or private settings.

`.local/` is intentionally ignored by Git and may be a symlink to private cloud
storage. Do not commit real Apple Team IDs, personal bundle identifiers,
certificates, provisioning profiles, or release-only notes.

